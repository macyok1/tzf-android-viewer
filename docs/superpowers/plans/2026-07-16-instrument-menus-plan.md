# Instrument menus implementation plan

1. Add reusable graphite modal-card and anchored-action-menu components plus drawable tokens.
2. Replace workspace `⋯` with icon/title/subtitle/state rows.
3. Redesign project creation/copy/rename dialogs and project cards with overflow actions.
4. Replace scan-tree and registration context menus.
5. Move stitching, ASC, confirmations, and X7 choice/input flows onto the shared modal shell.
6. Run JVM/build tests and phone/tablet emulator visual smoke, then publish and verify nightly.
