# Workspace stitching and ASC redesign plan

1. Add pure screen-pan math tests and replace orientation-dependent hand-written PAN basis.
2. Add draggable floating ORBIT behavior and clean toolbar/X7 visibility.
3. Extend registration tree state with runtime R/M pair selection and a three-mode bottom action.
4. Implement link-component split when detaching a station, with graph tests.
5. Add pure ASC thinning/filter policies, dialogs, streaming export, and locked-CUT handling.
6. Run JVM/build/device smoke, push `main`, and verify nightly metadata.

