# Исследование формата TZF

Этот документ фиксирует только проверенные факты и явно отделяет их от гипотез.

## Тестовый файл

- Файл: `Scan_001.tzf`
- Размер: `54 546 415` байт
- Источник эталонных данных: Trimble RealWorks 12.0
- Известная сетка: `1742 x 4123`
- Известное число валидных точек: `5 524 662`

## Каталог блоков по адресу `0x2690F88`

Уверенность: высокая. Поля прочитаны непосредственно из TZF, а offsets нескольких блоков подтверждены ETW-трассой RealWorks.

```text
+0x00  8 bytes   неизвестная сигнатура/идентификатор
+0x08  u32 LE    200
+0x0C  u32 LE    1764 — размер payload
+0x10  u32 LE    4 — число компонентов (первое поле payload)
+0x14             начало component[0]
```

Payload полностью описывается формулой:

```text
u32 componentCount
repeat componentCount times:
    u32 componentId
    u32 descriptorCount
    descriptor descriptors[descriptorCount]
```

Дескриптор занимает 12 байт:

```text
+0x00  u64 LE    offset блока в TZF
+0x08  u32 LE    размер/длина блока
```

В тестовом файле четыре компонента с ID `1..4`; каждый содержит 36 дескрипторов. Размер структуры совпадает до байта: `4 + 4 x (8 + 36 x 12) = 1764`. Все 144 дескриптора проверяются относительно границ TZF.

36 блоков являются тайлами `512 x 512`: `ceil(1742 / 512) x ceil(4123 / 512) = 4 x 9 = 36`. Индекс тайла вычисляется как `tileX * 9 + tileY`. Для строки скана 1000 `tileY = 1`, поэтому `TZF_GetXYZ` читает тайлы `1, 10, 19, 28` компонентов 1–3. Это подтверждено ETW-трассой.

Первые три дескриптора:

```text
0: offset=0x16FEF8, size=524358
1: offset=0x659E6F, size=654719
2: offset=0xB470E9, size=814328
```

Offsets `0x16FEF8`, `0x659E6F`, `0x4AD314`, `0x58FD14` и `0x8F38D7` непосредственно присутствуют в сохранённой ETW-карте чтений. Остальные блоки могли не читаться при запросе одного столбца.

## Заголовок тайла

Уверенность: высокая для смещений и размеров полей; назначение scale и codecId подтверждается дополнительно.

```text
+0x00  u32 LE    marker = 100
+0x04  u32 LE    width = 512
+0x08  u32 LE    height = 512
+0x0C  u32 LE    размер распакованного канала
+0x10  f32 LE    масштаб: 10.0, 1000000.0 или 1.0
+0x14  u64 LE    идентификатор кодека
+0x1C             начало сжатого payload
```

Компоненты 1–3 имеют распакованный размер `512 x 512 x 4 = 1 048 576` байт. Компонент 4 имеет размер `512 x 512 = 262 144` байт и его payload начинается с JPEG/JFIF. Следовательно, компонент 4 хранит одноканальное изображение, вероятно интенсивность или яркость цвета; точное назначение требует сравнения с RealWorks.

Идентификаторы на тестовом файле:

```text
component 1: 0x839A721B429840B9, scale 10.0
component 2: 0xAFEC6C11BC26B6C5, scale 1000000.0
component 3: 0xAFEC6C11BC26B6C5, scale 1000000.0
component 4: 0xB2EBC9519992D032, scale 1.0, JPEG/JFIF
```

Сопоставление с экспортами `GetGUID` из `ZZCodecs_x64.dll`:

```text
0x839A721B429840B9 = SnappyChunkCodec
0xAFEC6C11BC26B6C5 = Snappy_Transpose_DeriveChunkCodec
0xB2EBC9519992D032 = JPEGChunkCodec
```

`Snappy_Transpose_DeriveChunkCodec::_Decode` выполняет:

1. raw Snappy decode;
2. накопительную сумму `uint32` по всему массиву с 32-битным переполнением;
3. транспонирование: `dst[y * width + x] = src[x * height + y]`.

Порядок восстановлен непосредственно по машинному коду DLL и воспроизведён в `tzf-core` без использования DLL во время работы.

## Семантика компонентов и сборка XYZ

Уверенность: высокая. Сырые значения независимо распакованы и сравнены с `TZF_GetSpherical` и `TZF_GetXYZ` для строки 1000.

```text
component 1: rho * 10
component 2: polar * 1 000 000
component 3: azimuth * 1 000 000
component 4: JPEG-изображение яркости/интенсивности
```

Несмотря на физическое транспонирование компонентов 2/3 при декодировании, семантический индекс точки во всех каналах остаётся `localX * tileHeight + localY`. Для строки 1000 используется `localY = 1000 % 512 = 488`.

Первая валидная точка строки 1000:

```text
rho     = 168611 / 10       = 16861.1
azimuth = 2511274 / 1000000 = 2.511274
polar   = 1558 / 1000000    = 0.001558
```

Переход в декартовы координаты:

```text
x = rho * sin(polar) * cos(azimuth)
y = rho * sin(polar) * sin(azimuth)
z = rho * cos(polar)
```

Результат совпадает с RealWorks: `(-21.2216148, 15.4833479, 16861.0801)` в пределах округления `float32`.

Полная независимая сверка строки 1000:

```text
позиций: 1742
валидных точек: 1267
max error rho:     0.0012
max error azimuth: 1.3e-7 rad
max error polar:   1.2e-7 rad
max error XYZ:     0.0024
```

Эталонные сферические значения сохранены в `testdata/scan001-column1000-spherical.csv` и могут быть повторно созданы инструментом `tools/export_spherical_oracle_column.ps1`.

## Основной заголовок и автоматическое обнаружение

Следующие смещения подтверждены на 40 разных TZF-файлах:

```text
main +0x0C  u32  размер основного заголовка (76)
main +0x10  u64  offset scan-info
main +0x28  u64  offset конца основного контейнера
main +0x40  u64  offset каталога блоков

scan-info +0x30  u32  ширина скана
scan-info +0x34  u32  высота скана
scan-info +0x38  u32  размер тайла (512)
scan-info +0x3C  u32  число валидных точек
```

На всех 40 файлах каталог содержит четыре компонента, а число дескрипторов каждого компонента точно равно `ceil(width / 512) * ceil(height / 512)`. Поэтому CLI и библиотека могут автоматически находить каталог и геометрию без параметров, специфичных для тестового файла.

## Семейство Snap2

В более новых тестовых файлах обнаружены специализированные кодеки:

```text
0xA4FD4753B90FF7AC = FWHASnap2 (rho)
0xA0DA156D68656D0F = FWVASnap2 (polar)
0x93D2C23EDC95D64D = FWSDSnap2 (azimuth)
```

Имена восстановлены из скрытых фабрик кодеков в `ZZCodecs_x64.dll`. Нормализованный pipeline до конечных каналов, включая преобразования, которые RealWorks распределяет между кодеком и конвертером:

```text
FWHASnap2: raw Snappy -> transpose
FWVASnap2: raw Snappy -> derive отдельно по каждой строке -> transpose
FWSDSnap2: raw Snappy -> derive отдельно по каждой строке -> transpose
```

Угловые компоненты читаются как знаковые `int32`; это необходимо для отрицательных azimuth. Радиус остаётся `uint32`.

Полная сверка `FWHASnap2/FWVASnap2/FWSDSnap2` на другом TZF:

```text
геометрия: 2151 x 5493
тайлов на компонент: 55
строка: 1000
позиций: 2151
валидных точек: 1711
max error rho:     0.00007
max error azimuth: 1.2e-7 rad
max error polar:   1.2e-7 rad
max error XYZ:     0.00018
```

## Исправленная гипотеза

Предыдущая рабочая сессия трактовала `1764` как число 36-байтных записей. Проверка дала выходящие за файл offsets уже с предполагаемой записи 12. Полный разбор payload доказал, что `1764` является его размером, `4` — числом компонентов, а `36` внутри каждого компонента — числом дескрипторов.

## Следующие вопросы

1. Что обозначают четыре компонента с ID `1..4` и их 36 блоков?
2. Где каталог связывается с GUID и цепочками кодеков?
3. Включает ли размер дескриптора заголовок/трейлер блока или только полезные данные?
4. Где находятся параметры transpose, derive/delta, origin и scale?
